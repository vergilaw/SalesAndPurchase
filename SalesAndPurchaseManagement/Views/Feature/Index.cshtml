@model IEnumerable<SalesAndPurchaseManagement.Models.Feature>

@{
    ViewData["Title"] = "Danh Sách Tính Năng";
    Layout = "~/Views/Shared/MyLayoutHelper.cshtml";
}

<div class="ms-2 mx-2">
    <a asp-controller="Feature" asp-action="Create" class="btn btn-primary">
        <i class="fa fa-plus"></i> Tạo Tính Năng Mới
    </a>

    @if (!Model.Any())
    {
        <div class="alert alert-warning mt-3">
            Không có tính năng nào trong danh sách.
        </div>
    }
    else
    {
        <div class="card">
            <div class="card-header">
                <h3 class="card-title">Danh Sách Tính Năng</h3>
            </div>
            <div class="card-body">
                <table id="example1" class="table table-bordered table-striped">
                    <thead>
                        <tr>
                            <th>@Html.DisplayNameFor(model => model.First().FeatureId)</th>
                            <th>@Html.DisplayNameFor(model => model.First().FeatureName)</th>
                            <th>Sửa</th>
                            <th>Xóa</th>
                            
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var feature in Model)
                        {
                            <tr>
                                <td>@Html.DisplayFor(modelItem => feature.FeatureId)</td>
                                <td>@Html.DisplayFor(modelItem => feature.FeatureName)</td>
                                <td>
                                    <a asp-action="Edit" asp-route-id="@feature.FeatureId" class="btn btn-warning btn-sm me-1">
                                        <i class="fa fa-pencil-alt"></i> Sửa
                                    </a>
                                </td>

                                <td>
                                    <a asp-action="Delete" asp-route-id="@feature.FeatureId" class="btn btn-danger btn-sm me-1">
                                        <i class="fa fa-trash"></i> Xóa
                                    </a>
                                </td>

                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    }
</div>